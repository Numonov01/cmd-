import { Empty } from "antd";

function Dashboard() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
    >
      dashboard
      <Empty />
    </div>
  );
}

export default Dashboard;
